<% if e.errors.any? %>
  <h2><%= e.errors.count %> errors prohibited this obj from being saved:</h2>
  <ul>
    <% e.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>